# Assignment 2 final-submission checklist

Complete these items after the project is pushed and deployed:

- [ ] Create a GitHub repository and push every project file, including `model/artifacts/`, `metrics.csv` and `test_data.csv`.
- [ ] Replace the GitHub placeholder in `README.md` with the real repository link.
- [ ] Deploy `app.py` on Streamlit Community Cloud and replace the Streamlit placeholder in `README.md` with the live link.
- [ ] Run the complete assignment on BITS Virtual Lab and capture one clear screenshot showing the execution.
- [ ] Copy the README content, GitHub URL, Streamlit URL and BITS Virtual Lab screenshot into a single PDF, in the order prescribed by the assignment brief.
- [ ] Open every submitted link in an incognito/private browser window before submitting.
